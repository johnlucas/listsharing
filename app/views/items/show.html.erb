<p>
  <%= @item.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', list_items_path(@list), :class => 'btn'  %>
  <%= link_to 'Edit', edit_list_item_path(@list,@item), :class => 'btn' %>
  <%= link_to 'Delete', list_item_path(@list,@item), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
